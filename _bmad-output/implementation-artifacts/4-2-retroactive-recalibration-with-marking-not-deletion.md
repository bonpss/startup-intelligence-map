---
baseline_commit: ce6655d6a98e93d5359b79af0cb5c9a6389ec5af
---

# Story 4.2: Retroactive recalibration with marking (not deletion)

Status: done

## Story

As a Julien,
I want to raise `COMPETITOR_THRESHOLD` and mark existing relationships that no longer qualify,
so that the graph reflects one consistent, stricter precision bar without losing the rejected relationships as future calibration data.

## Acceptance Criteria

1. **Given** a new, higher `COMPETITOR_THRESHOLD` value chosen after reviewing FR-1/FR-3 progress — **this story assumes a raised threshold; a lowered threshold would require a full LLM rescoring of previously-rejected candidate pairs (never persisted) and is explicitly out of scope** — **when** the constant is updated in `storage.py` (code change) and the recalibration script runs, **then** a migration (per AD-3: MCP `apply_migration` + versioned file) adds an `active` boolean column to `competitors`, default `true`.
2. **And** all existing rows are re-evaluated against the new threshold using their already-stored `score` — no new LLM calls needed (100% of rows have `score` populated, reconfirmed live 2026-08-15: 0 of 4,707 rows have a null `score`).
3. **And** rows whose score falls below the new threshold are marked `active=false`, never deleted — preserved as potential future input to a `review_type='competitor_score_audit'` entry in `quality_review_log`.
4. **And** rows still meeting the new threshold remain `active=true`, unchanged.
5. **And** every consumer that surfaces relationships as *currently valid* filters on `active=true` — confirmed by full code audit (grep), not assumed: `graph_app.py:104` (`/api/graph/all`), `graph_app.py:141-142` (`/api/graph/{name}`), `graph_analysis.py:49` (`fetch_edges()`, otherwise inactive links would also skew community/betweenness detection), `competitor_validator.py:68` (**critical** — this is literally the SM-1 sampling path; unfiltered, it would validate a mix of active/inactive relationships and corrupt the precision metric FR-4 exists to produce), `competitor_validator.py:164` (the "remaining unchecked" counter, for consistency).
6. **And** `storage.py`'s `get_known_competitors()` and `relationship_exists()` deliberately stay **unfiltered** — their semantics are "already evaluated" (dedup), not "currently valid for display," and filtering them would cause a redundant LLM rescore of an already-known pair on a future ingest.

> **Deferred dependency, not a Story 4.2 concern:** if lowering the threshold is ever considered (out of scope today), `get_known_competitors()`/`relationship_exists()` staying unfiltered would then also block a legitimate rescoring of `active=false` pairs. Noted here so it surfaces as a known implicit dependency when that scenario is actually proposed, not rediscovered as a surprise.

## Tasks / Subtasks

- [x] Task 0: **Correct a stale reference before starting** (blocks Task 1)
  - [x] `epics.md`'s AC literally names the migration file `migrations/002_add_active_to_competitors.sql` — **this number is now taken.** `002_harden_quality_review_log_schema.sql` was added during Story 1.1's own code review, *after* Epic 4's epics.md text was written. Confirmed live (2026-08-15): `migrations/` contains `001_create_quality_review_log.sql` and `002_harden_quality_review_log_schema.sql`. This story's migration must be **`003_add_active_to_competitors.sql`** — AD-3's "numbers never reused" rule leaves no other option. Do not create a second `002_...` file.
- [x] Task 1: Migration — `active` column on `competitors` (AC: #1)
  - [x] Write `migrations/003_add_active_to_competitors.sql`: `ALTER TABLE competitors ADD COLUMN IF NOT EXISTS active boolean DEFAULT true;` — no `NOT NULL`, matching this table's own existing precedent for boolean flags (`checked`/`validated`, added by `competitor_validator.py`'s ad hoc migration, neither is `NOT NULL` either) — consistency with the table's existing shape over abstract strictness.
  - [x] Apply via the Supabase MCP `apply_migration` tool (AD-3), matching Story 1.1's precedent exactly. This step is schema-only and non-destructive — every existing row gets `active=true` by the column default, so applying it live changes no visible behavior yet. Safe to execute live during this story's own implementation, unlike Task 2 below.
  - [x] Verify live: the column exists, and a spot-check confirms existing rows read back `active=true` (the default applying retroactively to pre-existing rows, not just future inserts — confirm this Postgres behavior directly rather than assuming it).
- [x] Task 2: Build `recalibrate_competitors.py` (AC: #2, #3, #4)
  - [x] New flat script at repo root (matches `diagnose_scraping.py`/`readiness_check.py`'s convention — one script per concern, no `src/` nesting).
  - [x] Fetch **all** `competitors` rows (`id`, `company_a`, `company_b`, `score`, `active`), paginated at 1000 rows per page — reuse the exact pagination pattern already established in `graph_app.py:83-97`/`graph_analysis.py:44-59` (Supabase caps a single response at 1000 rows; both existing call sites already loop `range(page, page+size-1)` until a short page signals the end). Do not write a new pagination approach.
  - [x] Read the threshold to recalibrate against from **`storage.COMPETITOR_THRESHOLD`** directly — no CLI argument for it. This matches AC #1's literal sequencing ("the constant is updated in `storage.py`... **and** the recalibration script runs"): bumping the constant is a separate, prior code change (Julien's own call on the actual number — an open PRD question, §8 Q4, not something this story or its dev agent decides), and the script trusts whatever value is live in `storage.py` at the moment it runs.
  - [x] For each row: if `score < COMPETITOR_THRESHOLD` **and** currently `active=true`, update it to `active=false`. Rows already `active=false` are left untouched (idempotent re-runs don't re-write already-inactive rows), and rows with `score >= COMPETITOR_THRESHOLD` are left untouched (already `active=true` by default — no write needed, satisfies AC #4 without any code doing anything to them). The script is **one-directional by construction** — it only ever moves `true → false`, never the reverse — which is exactly right given AC #1's explicit "lowering is out of scope" constraint, without needing the script to somehow detect whether the threshold went up or down.
  - [x] Print a summary: total rows evaluated, count newly marked inactive, count remaining active, count already inactive from a previous run (if any) — matching the reporting style of every other Epic 2-4 script (`diagnose_scraping.py`, `readiness_check.py`).
- [x] Task 3: Add the `active=true` filter to every "currently valid" consumer (AC: #5)
  - [x] `graph_app.py:104` (`api_graph_all`'s `db.table("competitors").select("company_a, company_b, score")...` inside its pagination loop) — add `.eq("active", True)`.
  - [x] `graph_app.py:141-142` (`api_graph`'s two `db.table("competitors")...eq("company_a"/"company_b", name)` calls) — add `.eq("active", True)` to both.
  - [x] `graph_analysis.py:49` (`fetch_edges()`'s `client.table("competitors").select("company_a, company_b, score")` inside its pagination loop) — add `.eq("active", True)`.
  - [x] `competitor_validator.py:68` (the unchecked-rows sampling query, `.eq("checked", False)`) — add `.eq("active", True)` alongside the existing `checked` filter. **This one is the most consequential of the five**: it's the literal SM-1 precision-metric sampling path — validating a mix of active/inactive relationships here would corrupt the very metric FR-4 exists to produce.
  - [x] `competitor_validator.py:164` (the "remaining unchecked" summary count, `.eq("checked", False)`) — add `.eq("active", True)` alongside it too, for consistency (an inactive-but-still-unchecked row shouldn't inflate the "remaining work" count).
  - [x] Do **not** touch `storage.py`'s `get_known_competitors()` or `relationship_exists()` (AC #6) — no `.eq("active", ...)` added to either, and no other line in `storage.py` besides `save_relationships`' existing `COMPETITOR_THRESHOLD` comparison should reference `active` at all.

## Dev Notes

- **Blast-radius safety boundary — read before touching Task 2:** unlike every prior Epic 1-4 story, this one's write path is genuinely consequential at scale (4,707 live relationships as of 2026-08-15, all of which could be affected by one script run) and semi-hard-to-reverse in practice (rows aren't deleted, so it's *technically* reversible by flipping `active` back, but there's no code path that does that automatically, and the "what should be active again" answer depends on which threshold was in effect when). Because of this:
  - Task 1 (migration) is safe to apply live during this story's own dev-story session — schema-only, defaults every row to `active=true`, zero visible behavior change on its own.
  - Task 3 (filter additions) is safe to verify **live** against real data today — because every row currently has `active=true` (nothing has been recalibrated yet), adding `.eq("active", True)` to each of the 5 queries is a no-op right now: confirm each query returns the **same rows** before and after the filter is added, proving the filter doesn't accidentally exclude anything, without needing to have actually recalibrated anything first.
  - Task 2 (the recalibration script's actual logic) must be verified with **fake/mocked `competitors` data only** — do **not** run it against the real, live `competitors` table with a self-chosen threshold as part of "verifying this story works." Choosing the actual new `COMPETITOR_THRESHOLD` value is Julien's own call (PRD §8 Open Question #4 — "SM-1's exact precision threshold... not yet fixed," revisit condition explicitly tied to FR-4 starting, which is now), not something a dev agent should decide or execute live. This mirrors exactly how Stories 3.1/3.2 verified `log_review.py`'s verdict-writing logic against fake `save_quality_review()` calls rather than writing a synthetic human judgment into the real `quality_review_log`. Once this story is implemented and its script logic is verified against fakes, running it for real is a separate step Julien takes himself, after he bumps `storage.COMPETITOR_THRESHOLD` to a number he's chosen.
- **`save_relationships()` (`storage.py`) needs no change.** New inserts don't specify `active` in their payload; the migration's `DEFAULT true` applies automatically at insert time, satisfying "rows still meeting the new threshold remain active=true" for future ingests without any Python-side change.
- **Row count context:** the PRD's "3,780 relationships" figure (from Story 1.1's 2026-08-10 zero-cost check) is stale — reconfirmed live 2026-08-15 at **4,707** rows, still 100% `score`-populated. Ongoing ingestion between now and whenever this story's script actually runs for real means the exact count at execution time will differ again; the script must not hardcode any row-count assumption (it doesn't — it just paginates until a short page).
- No new dependency, no `requirements.txt` change. `_client` import from `storage.py` for the pagination reads/writes, matching every other Epic 2-4 script's convention (`diagnose_scraping.py`, `log_review.py`, `readiness_check.py`) rather than adding a new `storage.py` wrapper for a one-off batch operation.
- No test suite for this story beyond the fake-data verification described above — same no-test-suite convention as Stories 2.1/3.1/3.2/4.1; Story 1.2's unit test remains the sole, explicitly scoped exception.

### Project Structure Notes

- New file: `recalibrate_competitors.py` at repo root — flat, no `src/` nesting.
- New file: `migrations/003_add_active_to_competitors.sql` (see Task 0 — not `002`, that number is taken).
- **UPDATE** (not new) files, read in full before editing, only the specific lines named in Task 3 touched: `graph_app.py`, `graph_analysis.py`, `competitor_validator.py`.
- Do not modify: `storage.py`'s `get_known_competitors()`/`relationship_exists()`/`save_relationships()` (AC #6, and no change needed per the Dev Notes above), `main.py`, `taxonomy.py`, `extractor.py`, `log_review.py`, `readiness_check.py`, `diagnose_scraping.py`.

### References

- [Source: `_bmad-output/planning-artifacts/epics.md` Epic 4 / Story 4.2] — acceptance criteria this story implements (migration filename corrected per Task 0, everything else unchanged from the epics.md text).
- [Source: `_bmad-output/planning-artifacts/prds/prd-SM Project-2026-08-09/prd.md` §4.3 FR-4(b) scope, §7 SM-1, §8 Open Question #4] — retroactive re-scoring scope decision and the still-open exact-threshold question this story's script deliberately doesn't resolve.
- [Source: `_bmad-output/planning-artifacts/architecture/architecture-SM Project-2026-08-10/ARCHITECTURE-SPINE.md` AD-3, AD-6] — migration mechanism (MCP + versioned file); "recalibration re-evaluates all existing rows, not just future ingests" is AD-6's own binding rule, not just this story's restatement of it.
- [Source: `storage.py`, `graph_app.py`, `graph_analysis.py`, `competitor_validator.py`, `migrations/`, all read in full 2026-08-15] — confirmed every AC-cited line number is still accurate despite unrelated concurrent changes to these files; confirmed `002_harden_quality_review_log_schema.sql` already occupies the number epics.md's AC text assumed was free; confirmed live row count (4,707) and 100% `score` population.
- [Source: `_bmad-output/implementation-artifacts/1-1-...md`] — precedent for applying a migration live via the Supabase MCP `apply_migration` tool during a dev-story session.
- [Source: `_bmad-output/implementation-artifacts/3-1-...md`, `3-2-...md`] — precedent for verifying a consequential human-judgment write path against fakes rather than a real write during story verification; this story applies the same principle to the threshold-recalibration write path.

## Dev Agent Record

### Agent Model Used

claude-sonnet-5

### Debug Log References

- No pytest suite for this story beyond the fake-data checks described in Dev Notes. Verified via: (1) clean import of all 14 project modules (added `recalibrate_competitors` to the roster), no regressions; (2) `pytest test_storage.py` — 29/29 still passing, untouched.
- Migration applied live via Supabase MCP `apply_migration` (`add_active_to_competitors`, version `20260815191438`, confirmed via `list_migrations`). Verified live immediately after: 4,707/4,707 rows read back `active=true`, 0 `active=false`, 0 `active=null` — confirms Postgres applies a column `DEFAULT` retroactively to pre-existing rows, not just future inserts.
- `recalibrate_competitors.py`'s core logic verified with **fully fake `_client()`/`_fetch_all_rows()`** (per the story's explicit blast-radius safety boundary — never run against the real table with a self-chosen threshold as part of story verification): a 4-row fixture (below-threshold+active, below-threshold+already-inactive, above-threshold, exactly-at-threshold) confirmed only the below-threshold+active row was updated (`newly_inactive=1`), the already-inactive row was correctly left alone (no duplicate write), the at-threshold row correctly stayed active (`>=` boundary, not `>`), and `threshold_used` correctly reflected the monkeypatched `COMPETITOR_THRESHOLD` at call time.
- `_fetch_all_rows()`'s pagination verified separately with a fake 3-page fixture (1000/1000/250 rows) — confirmed it fetches all 2,250 rows across exactly 3 calls (`range` offsets `0, 1000, 2000`) and stops on the short final page, matching `graph_app.py`/`graph_analysis.py`'s existing pattern.
- **Task 3's 5 filter additions verified live against the real table** (safe, since every row is `active=true` today — nothing has been recalibrated yet, so the filter is a guaranteed no-op): each of the 5 query shapes (`graph_app.py` `api_graph_all`/`api_graph`, `graph_analysis.py` `fetch_edges`, `competitor_validator.py`'s sampling query and remaining-unchecked counter) returned **identical results with and without** `.eq("active", True)` — including a non-trivial case (`Lorica.ai`, 9 real relationships, 9/9 before and after).

### Review Findings

- [x] [Review][Patch] `extractor.py`'s Story 3-1-round exemption fix (`orig & HORIZONTAL_SUBSECTORS`) was still wrong for a case distinct from the one it fixed: a sector whose original subs mixed a horizontal tag with a *real* subsector later demoted by `demote_generic_erp_tag` (e.g. `{"Speech & Audio AI", "ERP & Business Operations"}`) was incorrectly exempted from removal — the ERP tag's removal reason is cross-sector redundancy, not "removed because horizontal," same underlying category as the original bug. Reproduced live, fixed by checking `(orig - {"Uncategorized"}).issubset(HORIZONTAL_SUBSECTORS)` instead of `orig & HORIZONTAL_SUBSECTORS`, and extracted into a new, independently-documented `_sector_exempt_from_removal()` helper (this exact line has now needed correcting twice across two review rounds — worth a named, testable unit instead of another inline tweak). Re-verified against all 4 known scenarios (lone Uncategorized / pure horizontal / horizontal+Uncategorized / horizontal+ERP), all pass. [extractor.py]
- [x] [Review][Patch] The LinkedIn URL recovered by `_linkedin_url_from_html` (added last round) was appended to the *end* of `text`, but `extractor.py:216` truncates `markdown` to its first 30,000 characters — any page whose trafilatura-extracted text reaches that length would silently lose the LinkedIn suffix before the LLM ever saw it. Proven with a synthetic 30k-char string (old suffix approach: dropped; new prefix approach: survives). Fixed by prepending instead of appending. [main.py, `_parse_light_fetch`]
- [x] [Review][Patch] `_attr()` (added last round to dedupe HTML attribute parsing) matched an attribute name as a substring, so `_attr(tag, "src")` could return a `data-src="..."` value instead of the real `src="..."` on the same tag — confirmed live with a two-attribute fixture. More exercised now than before, since `_fetch_light` scans raw un-rendered HTML (where lazy-load `data-src` attributes are common) rather than Playwright's post-render DOM. Fixed with a negative lookbehind requiring a word/hyphen boundary before the attribute name. [main.py, `_attr`]
- [x] [Review][Patch] `storage.normalize_domain()` detected "has a scheme" via `"://" not in url` — true for any string containing `://` *anywhere*, not just at the start. A scheme-less URL with a redirect/next-URL query parameter (e.g. `example.com/path?redirect=http://other.com`) skipped the `https://` prepend and `urlparse()` then found no hostname at all, returning `""`. Confirmed live. Fixed with an anchored scheme regex (`^[a-zA-Z][a-zA-Z0-9+.\-]*://`); re-verified against all 10 previously-covered `normalize_domain` edge cases plus the new one, no regressions. [storage.py, `normalize_domain`]
- [x] [Review][Clarify, not a bug] Two independent review agents flagged `storage.py`'s `get_known_competitors()`/`relationship_exists()` staying unfiltered on `active` as a "one-way data trap" (a deactivated pair can never be reconsidered on re-ingest). **This is Story 4.2's own AC #6, not an oversight** — epics.md's text explicitly names this exact tradeoff in its "Deferred dependency" callout, deliberately deferred until threshold-lowering is ever proposed. No code change; documented here so it isn't re-flagged as new in a future review without the context.
- [x] [Review][Dismiss] The new `.eq("active", True)` additions in `graph_app.py`/`graph_analysis.py` call `.execute()` directly rather than through `storage.py`'s private `_execute()` retry wrapper. Accurate, but this is the pre-existing, project-wide convention for every Epic 2-4 script (`diagnose_scraping.py`, `log_review.py`, `readiness_check.py`, `recalibrate_competitors.py` — none use `_execute()`, only `storage.py`'s own internal functions do) — not something this story introduced or should fix in isolation.
- [x] [Review][Defer to Epic 5] Several findings duplicate ones already triaged into Epic 5 (Stories 5.1-5.3) from the previous review round: retry-config duplication across `storage.py`/`competitor.py`/`extractor.py`/`competitor_validator.py` (with `competitor_validator.py` confirmed still un-widened); `get_quality_reviews()`/`_validate_review()` validation duplication; `diagnose_scraping.py`'s `content_drowned_in_noise` heuristic broken by trafilatura's plain-text output; the widened retry backoff's up-to-~16-minute `/api/ingest` hang risk; `_fetch_light`'s missing anti-bot/interstitial detection; and the `asyncio.to_thread`-enabled concurrent-write race in `save_startup()`/`save_relationships()` (this round's cross-file tracer found the same race Julien already scoped into Epic 5 Story 5.2, with the concrete mechanics of `save_startup()`'s SELECT-then-INSERT-or-UPDATE as one example of it). Not re-litigated here.

**Confirmed by Julien in real usage after the fixes** (not a mock): `graph_app.py` run locally, graph loads and renders normally for a startup with real competitor relationships; a real startup ingested through the search bar with LinkedIn, logo, and favicon all coming back correctly — exercising 3 of the 4 patched bugs (LinkedIn-truncation fix, `_attr()` boundary fix, and the unaffected favicon path) against a live page, not synthetic data.

### Completion Notes List

- Task 0 correction confirmed necessary and applied: migration created as `migrations/003_add_active_to_competitors.sql`, not the `002_...` name epics.md's AC text literally specified (that number was already taken by Story 1.1's own follow-up hardening migration).
- New file `recalibrate_competitors.py` implements Task 2 exactly as scoped: reads `storage.COMPETITOR_THRESHOLD` directly (no CLI argument), one-directional (`true → false` only), paginated fetch reusing the established 1000-row-cap pattern, summary report matching the Epic 2-4 script convention.
- Task 3 touched exactly the 5 lines named in the story, in `graph_app.py`, `graph_analysis.py`, `competitor_validator.py` — no other line in any of the three files changed.
- `storage.py`'s `get_known_competitors()`, `relationship_exists()`, and `save_relationships()` are untouched by this story's own scope (AC #6) — the migration's `DEFAULT true` handles future inserts without any Python-side change. `storage.py` *was* touched during the Review Findings pass below (an unrelated pre-existing `normalize_domain()` bug, not part of Story 4.2's own scope).
- **This story's own script was deliberately not run against production data with a real threshold choice** — that remains Julien's own decision and action (PRD §8 Open Question #4, still open). The mechanism is built and verified; exercising it for real is an operational step outside this story's scope.
- No `requirements.txt` change, no modification to `taxonomy.py`, `log_review.py`, `readiness_check.py`, `diagnose_scraping.py`. `main.py` and `extractor.py` were touched only during the Review Findings pass below (pre-existing bugs found in last round's code, unrelated to Story 4.2's own recalibration scope).

### File List

- `migrations/003_add_active_to_competitors.sql` (new)
- `recalibrate_competitors.py` (new)
- `main.py` (modified in Review Findings pass — LinkedIn suffix moved to prefix; `_attr()` attribute-boundary fix)
- `storage.py` (modified in Review Findings pass — `normalize_domain()` scheme-detection fix)
- `extractor.py` (modified in Review Findings pass — `_sector_exempt_from_removal()` extracted with a corrected condition)
- `graph_app.py` (modified — `active=true` filter added to `api_graph_all`'s and `api_graph`'s `competitors` queries)
- `graph_analysis.py` (modified — `active=true` filter added to `fetch_edges()`)
- `competitor_validator.py` (modified — `active=true` filter added to the unchecked-rows sampling query and the remaining-unchecked summary count)

## Change Log

- 2026-08-15: Story implemented. Corrected a stale migration-number reference in epics.md's own AC text before starting (`002` already taken by Story 1.1's follow-up migration; used `003`). Applied `migrations/003_add_active_to_competitors.sql` live via Supabase MCP — confirmed all 4,707 existing rows retroactively read `active=true` from the column default. Built `recalibrate_competitors.py`: reads `storage.COMPETITOR_THRESHOLD` directly, paginates all `competitors` rows, marks `active=false` one-directionally for rows below threshold that are still `active=true`, leaves everything else untouched, prints a summary. Added `.eq("active", True)` to the 5 consumer call sites that surface relationships as currently valid (`graph_app.py` x2, `graph_analysis.py` x1, `competitor_validator.py` x2) — left `storage.py`'s `get_known_competitors()`/`relationship_exists()` deliberately unfiltered per AC #6. Verified the script's logic against fake data only (never run against real data with a self-chosen threshold, per this story's explicit blast-radius safety boundary); verified the 5 filter additions live against real data (safe no-op today, since nothing has been recalibrated yet). No regressions (29/29 `test_storage.py`, all 14 modules import cleanly). All 6 acceptance criteria met — the actual threshold-recalibration run itself remains Julien's own future action.
- 2026-08-15: Code review (`code-review` skill, multi-agent) — 4 confirmed bugs fixed, all pre-existing in code from the *previous* review round rather than in this story's own new code: (1) `extractor.py`'s sector-removal exemption was still wrong for a horizontal-tag-plus-demoted-ERP-tag case distinct from the bug fixed last round — corrected and extracted into a named `_sector_exempt_from_removal()` helper, re-verified against all 4 known scenarios; (2) the LinkedIn URL recovered by `_linkedin_url_from_html` was appended after `extractor.py`'s 30,000-char truncation point and would silently vanish on long pages — moved to a prefix, proven with a synthetic 30k-char string; (3) `_attr()` matched attribute names as substrings (`data-src` read as `src`) — fixed with a boundary lookbehind; (4) `storage.normalize_domain()` mis-detected a scheme via unanchored `"://" in url`, losing the domain entirely for scheme-less URLs with a redirect query param — fixed with an anchored scheme regex, re-verified against all 10 previously-covered edge cases plus the new one. One finding clarified as already-deliberate (AC #6's unfiltered `get_known_competitors()`/`relationship_exists()`, already documented in epics.md, not a gap). Several findings confirmed as duplicates of ones already triaged into Epic 5 last round — not re-litigated. No regressions after the fixes (29/29 `test_storage.py`, all 14 modules import cleanly, live smoke test against a real page still correct).
