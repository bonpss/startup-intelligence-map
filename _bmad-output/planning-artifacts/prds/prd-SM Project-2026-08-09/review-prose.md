| Original Text | Revised Text | Changes |
|---|---|---|
| Realizes JTBD "spot a given startup's competitors", potentially "understand a sector before a decision". (§2.3, UJ-1) | Realizes JTBD "spot a given startup's competitors," potentially "understand a sector before a decision." | The document's own convention elsewhere (e.g. §4.2, §6.1, §8) puts closing punctuation inside the quotation mark; this is the only place it's outside — fixed for internal consistency (2 instances, same line). |
| — this origin is why both exist (§1) | Consider: — this origin is why both the comparison and the priority exist? | "Both" sits after two cross-references separated by an em dash, making its two antecedents (the §5 comparison, the §2.1 priority) harder to resolve on first read. Flagged as a query, not a definitive change, since the current phrasing is resolvable with a moment's thought. |
| the same ambiguity at Step 2a/2b classification (§4.2, FR-3 Notes) | the same ambiguity in Step 2a/2b classification | "At a classification stage" reads more naturally than "at a classification" for a process step; minimal preposition fix. |
| it must instrument how the fracture-type trace (isolated mis-tag vs. structural gap) gets captured per subsector (§4.6, Feature F description) | Consider: (isolated mis-tag / structural gap / scraping artifact / ambiguous) — matching FR-5's full verdict set? | This line still names only 2 of the 4 verdict values FR-5 (§4.6) later defines. Not a content change — just bringing this earlier mention in sync with the fuller list established downstream, so a reader doesn't form an incomplete mental model before reaching FR-5. |
| Schema is deliberately generic (not taxonomy-specific) so it can extend to future quality loops beyond C (§4.6, FR-5 "Format decision") | The schema is deliberately generic (not taxonomy-specific) so it can extend to future quality loops beyond C | Missing definite article at sentence start. |
