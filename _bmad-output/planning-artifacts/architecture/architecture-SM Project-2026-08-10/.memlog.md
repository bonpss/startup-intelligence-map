---
scope: SM Project -- the A/C/B/F pipeline-stabilization work from prd.md (FR-1 scraping diagnostics, FR-5 quality_review_log + migration, FR-3 taxonomy fracture queue, FR-4 competitor recalibration), on the existing brownfield Python/FastAPI/Supabase codebase
purpose: Dual-use: Julien's own resumption guide across work sessions, AND a showable artifact for technical interviews (Chief of Staff / AI Ops roles) -- spine must carry a short Why per AD, not just the terse decision
altitude: initiative -> features (feeds bmad-create-epics-and-stories next)
updated: 2026-08-10T15:24
---

- (decision) Paradigm ratified [ADOPTED]: pipeline / shared-core-library. 4 core modules (storage.py, taxonomy.py, extractor.py, competitor.py) imported by 8 independent entry-point scripts + 1 FastAPI server. No formal layers, no ORM, no framework beyond FastAPI.
- (constraint) Reality check: Supabase CLI not installed on this machine (which supabase -> not found). No existing migrations/ dir or ORM anywhere in the codebase.
- (decision) FR-5 migration mechanism decided: hybrid, not pure CLI nor pure hand-rolled script. Apply schema SQL via the Supabase MCP tool (apply_migration) already available in this working environment -- zero new dependency to install. ALSO save the executed SQL as a versioned file in-repo (migrations/001_create_quality_review_log.sql or similar) for git-side traceability independent of Supabase's own platform history. Reason: MCP alone only tracks on Supabase's side, not in git -- insufficient given the whole point of quality_review_log existing is to avoid the silent schema-drift pattern from competitor_validator.py's untracked raw-SQL RPC. Supabase CLI deferred as a future option if migration cadence increases beyond this single table.
- (direction) User consistently reasons from PRD's own founding principles when making architecture calls (e.g. schema-drift risk mirrors quality_review_log's own raison d'être) -- keep surfacing how each AD choice does or doesn't uphold that traceability principle.
- (decision) quality_review_log access goes through storage.py (new save_quality_review() function), no separate module. Reason: preserves the existing single-point-of-DB-access convention; a dedicated module only justified if quality-loop logic grows substantially beyond a verdict write. Revisit condition: if F's logic outgrows a simple write.
- (decision) Verdict-writing mechanism is a separate script (log_review.py), not an interactive mode bolted onto graph_analysis.py. Reason: the verdict is a human decision following read+discuss+verify (FR-3 Precondition 2 reading rubric), not an immediate byproduct of the report -- decoupling avoids pressuring a rushed verdict during report reading, consistent with FR-3's hard precondition on verdict-criterion consistency. graph_analysis.py stays strictly read-only.
- (constraint) Verified via Supabase MCP execute_sql on project umxlmpyxiujyzpkxxkmr (matches .env SUPABASE_URL): SELECT count(*) FILTER (WHERE score IS NULL), count(*) FROM competitors -> 0 null / 3780 total. Score column is 100% populated.
- (decision) FR-4 retroactive re-scoring: CONFIRMED IN SCOPE, zero-cost since score is populated on all 3780 existing competitor rows (verified live). Threshold recalibration re-evaluates existing relationships against the new threshold, not just future ingests -- no silent heterogeneity between 'updated' and 'stale' relationships in the graph.
- (decision) diagnose_scraping.py: separate read-only script, same decoupling logic as log_review.py/graph_analysis.py -- diagnosis never mixed with correction.
- (decision) diagnose_scraping.py results write to quality_review_log (review_type='scraping_diagnostic') from v1, NOT a local ephemeral JSON report. Reason: FR-3's 'scraping artifact' verdict exists specifically so a taxonomy-fracture diagnosis can cross-reference known scraping issues -- an ephemeral local report would recreate exactly the problem already fixed for C (graph_analysis_report.json being overwritten each run).
- (decision) Schema nuance: unlike taxonomy_split's verdict (fixed 4-value set), scraping failure-type taxonomy is not yet known (still to be diagnosed) -- verdict for review_type='scraping_diagnostic' stays free text initially, structures organically as diagnoses accumulate. Implication: quality_review_log.verdict must be a plain text column (not a DB-level ENUM/CHECK), with the 4-value constraint for taxonomy_split enforced at the application layer (save_quality_review() in storage.py / log_review.py), not the schema layer -- a hard DB constraint would break scraping_diagnostic's free-text entries.
- (decision) Deployment/environments envelope: explicitly captured as Deferred, not silently omitted. Same justification as PRD: no deployment config exists, no distinct dev/prod environments, /api/ingest auth unaddressed -- all out of v1 scope, consistent with the multi-user Non-Goal already locked in the PRD.
- (version) Verified installed versions (live .venv, not web-guessed): Python 3.11.5, fastapi 0.136.3, uvicorn 0.49.0, mistralai 2.4.9, supabase-py 2.30.1, playwright 1.60.0, playwright-stealth 2.0.3, networkx 3.6.1, httpx 0.28.1, tenacity 9.1.4, python-dotenv 1.2.2, html2text 2025.4.15.
- (event) Load-bearing coaching complete -- moving to draft the remaining seed sections (conventions, stack, source tree) directly for user review, per user's explicit choice to skip elicitation on the factual sections.
- (change) Reconciliation against PRD via subagent: 4 gaps found and fixed. (1) AD-2 rule now explicitly states the getter must be queryable by review_type+subject so FR-3's Precondition 1 (scraping-artifact check) is architecturally enforceable, not just stated. (2) FR-4's Capability Map row now cross-references PRD's concrete start trigger as a process gate not re-derived here. (3) AD-3's Why corrected -- was conflating the table-vs-file rationale (PRD's, about querying for Scenario A/B) with the migration-mechanism rationale (schema-drift prevention); now cleanly separated. (4) Deferred section's extraction-rewrite item now names both Scenario A and B as open, not just A.
- (change) Reviewer Gate complete: lint_spine.py (0 findings after fix), rubric walker (conditional pass -> full pass), version-verification lens (plausible, no fabrication), adversarial-incompatibility lens (FAIL -> fixed). Fixed: AD-5 rewritten to resolve real contradiction (diagnose_scraping.py DOES write to quality_review_log directly, only taxonomy verdicts are decoupled via log_review.py); new AD-7 added (review_type/subject contract, diagnose_scraping.py progress-state via the table itself); AD-3 broadened (Binds beyond just quality_review_log, RLS-on-by-default rule, migrations/ naming convention, precedent strengthened with 7 verified prior migrations); Postgres version corrected to full 4-segment 17.6.1.084; Deferred section gained test-suite line + 3 dependency-version-risk notes (networkx, playwright-stealth, mistralai). AD-7 initially inserted out of order (caught by lint_spine.py's ad_id check), moved to correct sequential position. No user-facing blockers required discussion -- all findings were clear autofixes per Finalize's own gate discipline.
- (event) No open [ASSUMPTION] tags or open questions remain in the spine -- triage step is a no-op. No separate human-facing artifact requested (user confirmed spine alone serves the dual resumption-guide + interview-artifact purpose) -- Finalize step 5 (renderings) is a no-op, no doc_standards polish pass needed since it would only apply to a separate artifact that wasn't built.
- (event) spine finalized
- (decision) AD-8 added during epics-and-stories story writing (Update intent): domain normalization for quality_review_log.subject (review_type='scraping_diagnostic') must be a single shared function in storage.py, not reimplemented independently by diagnose_scraping.py (Epic 2, Story 2.1) and log_review.py (Epic 3, Story 3.2). Found because two independently-built stories could silently diverge on domain format (https:// / www. / trailing slash variants), making Story 3.2's scraping-artifact guardrail fail to match entries that actually exist -- worse than no guardrail, a false sense of safety.
